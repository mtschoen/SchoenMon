package com.example.perfstream.ui.xr

import androidx.compose.runtime.Composable

/**
 * Galaxy XR spatial layout. Stub in Task A3 (keeps the gate compilable); Task A4
 * fills it with the dashboard floating as a grabbable [SpatialPanel], and Phase B
 * adds the per-core ridgeline surface beside it.
 */
@Composable
fun SpatialDashboard() {
    // Filled in by Task A4.
}
